Configuration:
  Appenders:
    Console:
      name: CONSOLE
      PatternLayout:
        pattern: "%d [%t] %5highlight{%p}{FATAL=bg_red, ERROR=red, WARN=yellow, INFO=green, DEBUG=blue, TRACE=magenta} %c{1.} - %m%n"
  Loggers:
    Root:
      level: INFO
      AppenderRef:
        ref: CONSOLE
